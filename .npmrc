engine-strict=true
auto-install-peers=true
strict-peer-dependencies=false
save-exact=true

# Vibe Auth client (private, GitHub Packages). The token never lives here:
# developers keep it in ~/.npmrc, image builds mount NODE_AUTH_TOKEN as a
# BuildKit secret, Actions uses GITHUB_TOKEN.
@kisaesdevlab:registry=https://npm.pkg.github.com
